import React from 'react';
import logo from '../../face-detection.svg';

const Logo = () => {
    return (
        <>
            <img src={logo} alt={'logo'} height={56} width={56} />
        </>
    );
};

export default Logo;
